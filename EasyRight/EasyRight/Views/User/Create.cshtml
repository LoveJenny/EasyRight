@model EasyRight.Models.ERUser

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm())
{
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>ERUser</legend>

        @Html.EditorForModel()
         
		<div class="editor-label">
             @Html.LabelFor(model => model.Properties)
         </div>
        <div class="editor-field">
            <fieldset>
                <legend>Additional Properties
                </legend>
                <div>
                    @Html.EditorFor(model => model.Properties)
                </div>
            </fieldset>

            @Html.ValidationMessageFor(model => model.Properties)
        </div>
        
        @Html.Action("SelectUserRoles", "Role", new { user = Model })

        <p>
            <input type="submit" value="Create" />

			@Html.ActionLink("Back to List", "Index")
        </p>
    </fieldset>
}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
