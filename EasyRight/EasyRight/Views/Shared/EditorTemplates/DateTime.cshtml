@model string

@{
    DateTime dt = string.IsNullOrEmpty(Model) ? DateTime.Today : Convert.ToDateTime(Model);    
}

@Html.TextBox("", String.Format("{0:yyyy-MM-dd}", dt), new { @class = "ui_date_picker" })